<?xml version="1.0" encoding="utf-8" ?>
<xct:Popup xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             Size="300, 200"
             xct:CornerRadiusEffect.CornerRadius="50"
             x:Class="Air.Book">

    <StackLayout VerticalOptions="CenterAndExpand">
        <Label Text="Are You Sure?" HorizontalOptions="CenterAndExpand" FontSize="20" FontFamily="Bold" TextColor="Black" Margin="0, 20, 0, 0"/>
        <Label Text="* The host of the selected ride will be notified, but" HorizontalOptions="Center" FontSize="10" FontFamily="Medium" TextColor="Gray" Margin="0, 10, 0, 0"/>
        <Label Text="booked rides can be cancelled anytime and anywhere" HorizontalOptions="Center" FontSize="10" FontFamily="Medium" TextColor="Gray" Margin="0, -5, 0, 0"/>

        <StackLayout Orientation="Horizontal" HorizontalOptions="CenterAndExpand">
            <Button CornerRadius="10" WidthRequest="100" BackgroundColor="Gray" Margin="30, 20, 0, 0" Text="Cancel" TextColor="White" FontSize="15" FontFamily="Medium" Clicked="Cancel_Clicked"/>
            <Button CornerRadius="10" WidthRequest="100" BackgroundColor="#9dbfab" Margin="20, 20, 30, 0" Text="Book" TextColor="White" FontSize="15" FontFamily="Medium" Clicked="Redeem_Clicked"/>
        </StackLayout>
    </StackLayout>

</xct:Popup>    
